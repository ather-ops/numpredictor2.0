# numpredictor2.0
A complete Multiple Linear Regression model built from scratch using Python and NumPy. Includes manual gradient descent, mini-batch training, hyperparameter tuning, loss visualization, parameter tracking, and real house-price prediction using a custom dataset.
